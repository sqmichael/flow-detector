<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext WebSocket (ws://) to any user-specified relay server IP.
         The app only connects to a single IP entered by the user in the UI. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
